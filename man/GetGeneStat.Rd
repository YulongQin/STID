% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpotDetect.R
\name{GetGeneStat}
\alias{GetGeneStat}
\title{Calculate summary statistics for selected genes}
\usage{
GetGeneStat(
  STID_obj = NULL,
  pattern = NULL,
  features = NULL,
  prefix = NULL,
  func = "sum",
  assay_id = "Spatial",
  layer_id = "counts"
)
}
\arguments{
\item{STID_obj}{A Seurat or STID object}

\item{pattern}{Character, regular expression pattern to select genes (default: NULL)}

\item{features}{Character vector of specific gene names to include (default: NULL)}

\item{prefix}{Character, prefix for output column names}

\item{func}{Character, function to apply for nCount calculation. Options:
"sum", "mean", "median", "max", "min" (default: "sum")}

\item{assay_id}{Character, name of the assay to use (default: "Spatial")}

\item{layer_id}{Character, name of the layer/data slot to use (default: "counts")}
}
\value{
A data frame with two columns:
\item{prefix_nCount(func)}{Summary statistic for total counts}
\item{prefix_nFeature(sum)}{Number of detected features}
}
\description{
This function computes summary statistics (nCount and nFeature) for a set of
selected genes, useful for creating pathogen-specific metrics.
}
\examples{
\dontrun{
# Calculate pathogen gene statistics
pathogen_stats <- GetGeneStat(
  STID_obj = STID_object,
  pattern = "^MTB",
  prefix = "Pathogen",
  func = "sum"
)

# Add to metadata
STID_object <- AddMetaData(STID_object, pathogen_stats)
}
}
