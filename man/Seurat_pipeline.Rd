% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Preprocessing.R
\name{Seurat_pipeline}
\alias{Seurat_pipeline}
\title{Standard Seurat Analysis Pipeline for Spatial Transcriptomics}
\usage{
Seurat_pipeline(
  seurat_obj = NULL,
  data_format = "stereo",
  data_type = "stRNA",
  resolution_index = seq(0.1, 1.3, 0.2),
  runTSNE_index = FALSE,
  assay_nm = NULL
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing spatial transcriptomics data}

\item{data_format}{Character, data format - "stereo" (Stereo-seq) or "visium"
(default: "stereo")}

\item{data_type}{Character, data type - "stRNA" (spatial) or "scRNA" (single-cell)
(default: "stRNA")}

\item{resolution_index}{Numeric vector, clustering resolutions to test
(default: seq(0.1, 1.3, 0.2))}

\item{runTSNE_index}{Logical, whether to perform t-SNE (default: FALSE)}

\item{assay_nm}{Character, assay name (default: NULL, auto-detected)}
}
\value{
A Seurat object with processed data and cluster annotations
}
\description{
Performs a complete Seurat analysis pipeline for spatial transcriptomics data,
including normalization, feature selection, scaling, PCA, clustering, and
UMAP/t-SNE dimensionality reduction. Also generates spatial feature plots.
}
\examples{
\dontrun{
# Run standard pipeline on spatial data
seurat_obj <- Seurat_pipeline(
  seurat_obj = my_spatial_data,
  data_format = "stereo",
  resolution_index = seq(0.2, 1.0, 0.2)
)
}
}
