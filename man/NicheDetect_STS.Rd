% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NicheDetect.R
\name{NicheDetect_STS}
\alias{NicheDetect_STS}
\title{Automated ROI detection using density-based spatial clustering}
\usage{
NicheDetect_STS(
  STID_obj = NULL,
  meta_key = NULL,
  loop_id = "LoopAllSamp",
  pos_colnm = NULL,
  neg_value = "neg",
  spatial_scale_method = "region",
  region_detect_method = "convex",
  concavity = 2,
  update_spots = TRUE,
  density_thres = 0.9,
  ROI_size = 10,
  minPts = NULL,
  k_kNNdist = NULL,
  description = NULL,
  grp_nm = NULL,
  dir_nm = "M2_NicheDetect_STS"
)
}
\arguments{
\item{STID_obj}{An STID object containing spatial transcriptomics data}

\item{meta_key}{Character, metadata key containing positive spot information}

\item{loop_id}{Character, sample grouping identifier (default: "LoopAllSamp")}

\item{pos_colnm}{Character, column name containing positive spot labels}

\item{neg_value}{Character, value indicating negative/non-ROI spots (default: "neg")}

\item{spatial_scale_method}{Character, detection level - "region" or "spot" (default: "region")}

\item{region_detect_method}{Character, hull method - "convex" or "concave" (default: "convex")}

\item{concavity}{Numeric, concavity parameter for concave hull (default: 2)}

\item{update_spots}{Logical, whether to iteratively update positive spots
(default: TRUE)}

\item{density_thres}{Numeric, density threshold for filtering (0-1, default: 0.9)}

\item{ROI_size}{Integer, minimum number of spots to form an ROI (default: 10)}

\item{minPts}{Integer, minimum points parameter for DBSCAN (default: NULL,
automatically determined based on data format)}

\item{k_kNNdist}{Integer, k value for kNN distance calculation (default: NULL,
uses minPts if not specified)}

\item{description}{Character, description of the analysis (default: NULL)}

\item{grp_nm}{Character, group name for output organization (default: NULL, uses timestamp)}

\item{dir_nm}{Character, directory name for output (default: "M2_NicheDetect_STS")}
}
\value{
Returns the modified STID object with added metadata containing ROI labels,
distances to ROI centers, edge information, and region classifications
}
\description{
This function automatically detects regions of interest (ROIs) using
density-based clustering (DBSCAN) on spatial transcriptomics data. It includes
preprocessing steps like density filtering and iterative spot updating.
}
\examples{
\dontrun{
# Automatically detect ROIs based on positive spots
STID_obj <- NicheDetect_STS(
  STID_obj = ist_object,
  meta_key = "M1_SpotDetect_Gene_20240101",
  pos_colnm = "Label_geneA",
  density_thres = 0.9,
  ROI_size = 10
)
}
}
