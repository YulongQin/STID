# Get STID options

Returns a list of current STID options.

## Usage

``` r
get_STID_options()
```

## Value

A list of STID options

## Examples

``` r
if (FALSE) { # \dontrun{
opts <- get_STID_options()
print(opts$parallel_workers)
} # }
```
